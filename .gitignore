# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp

# Env/secrets (this app has none today, but never commit these if added later)
.env
.env.local

# Claude Code local machine-specific state (not the shared launch.json)
.claude/settings.local.json
